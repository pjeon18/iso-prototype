# Optional: enables the LLM-driven conversation partner.
# The key is read SERVER-SIDE by the Vite dev proxy (/api/partner) and never
# ships to the browser. Without it, the app silently uses scripted personas.
ANTHROPIC_API_KEY=
